query GetMetadataById($id: ID!) {
  metadataById(id: $id) {
    id
    fileName
    imageUrl
    categories
    exif {
      make
      model
    }
  }
}

query GetMetadata($dummy: Boolean!) {
  metadata(dummy: $dummy) {
    id
    fileName
    categories
    width
    height
    format
    size
    imageUrl
    exif {
      make
      model
    }
  }
}
